# No arguments: `git status`
# For certain gh CLI arguments: behave like gh CLI
# If git, behave like git
g() {
  if [[ $# -gt 0 ]]; then
    if [[ $1 == "oc" ]]; then # "g oc <sha>" for open commit
      gh browse $2
    else
      git "$@"
    fi
  else
    git status
  fi
}
